basePath: /
host: localhost:8080
info:
  contact: {}
  description: '...'
  title: Go API
  version: "1.0"
paths:
  /api/ping:
    get:
      consumes:
      - application/json
      description: do ping
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - example
swagger: "2.0"
